export interface Medicine {
  id: string;
  name: string;
  nextHour: string;
  description?: string;
  timesInDay?: string;
  qtsDay?: string;
  startTime?: string;
  startDate?: string;
  createdAt?: string;
  updatedAt?: string;
}
